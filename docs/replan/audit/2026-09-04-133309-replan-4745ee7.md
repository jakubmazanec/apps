---
skill: replan
version: 1.3.1
timestamp: 2026-09-04T13:33:09+02:00
model: opus
plan_hash: 4745ee7
plan_ref:
  kind: path
  value: apps/somewhere/docs/superpowers/specs/2026-09-04-timer-tween-completion-design.md
security_guidance:
  detected: true
  reason: log-fresh
autonomous_mode: false
lessons_injected_bytes: 0
lessons_truncated: false
---

## Plan summary (first 200 chars)
Timer and Tween completion: design. Timer and Tween own their completion (hook or {channel, event}), side-car entry types and EventEmit go, Sprite show options flatten, Scheduler and ECS systems only remove.

## Agents dispatched
| Agent | Verdict | Critical | Important | Minor |
|---|---|---|---|---|
| Codebase alignment | ISSUES FOUND | 2 | 5 | 5 |
| Best practices & architecture | ISSUES FOUND | 1 | 6 | 9 |
| Project standards | ISSUES FOUND | 5 | 6 | 8 |
| Feasibility, risks & test design | ISSUES FOUND | 5 | 9 | 6 |
| Fresh perspective | ISSUES FOUND | 1 | 4 | 5 |

Security perspective not dispatched: the plan is an internal refactor of a game-engine scheduler with no auth, user input, secrets or network surface. security-guidance detected active via a fresh `~/.claude/security/log.txt` (no plugin hooks dir, no `claude-security-guidance.md` files found).

## Per-agent findings

### Codebase alignment
**Critical**
- Option union is non-exclusive: `{duration, onComplete, channel, event}` compiles and the hook is silently dropped (verified with tsc 6.0.3). Fix: `never` witnesses, discriminate on `options.channel === undefined` (the `in` check stops narrowing with optional `never` props).
- Snippets drop live comments (tweenSystem, wallHitPopupSystem, Scheduler JSDoc, Tween field docs).
**Important**
- Sprite snippet elides `#isOneShotPlaying = false` in the reset block; `#hasWarnedLoopEmit` unaddressed; four "emit" comments go stale.
- playerActionSystem comments and two test comments go stale.
- Verification misses the browser project (Modal is the only real `Scheduler.tween` + `onComplete` consumer).
- Prettier reflows the one-line ternary.
- `#isFinished` must sort between `#from` and `#target` in Tween.
**Minor**
- Docs clean apart from the spec itself; type-only imports create no cycle; Scheduler.update behaviour preserved; screen-side tween can now push to a channel (YAGNI); Timer doc-comment rewrites widen the diff.

### Best practices & architecture
**Critical**
- Same union hole, verified: `{duration, channel}` pushes `undefined` onto a channel and blows up a frame later in an unrelated consumer; `{duration, event}` is a silent no-op; both variants drop the hook. Fix as above.
**Important**
- Comment the state-before-delivery ordering (load-bearing for a throwing hook).
- State the cross-side convention (ECS entries use the event variant) as a comment, not a type split.
- Tighten `update` docs (Timer returns false forever after finishing, Tween sticky true, at-most-once delivery).
- Reword, do not drop, "emit" comments and identifiers.
- Say explicitly that a completion-less instance is legal.
- Update `Scheduler.tween` JSDoc.
**Minor**
- Keep the duplicated `#complete` construction (rule of three); re-entrancy unchanged and already tested; throw blast radius identical; detached-channel debuggability a wash; keep Tween `#isFinished` as a class invariant; layering is type-only; `Tween<T>` into `Set<Tween<unknown>>` compiles; Timer.test gains an ECS dependency; Sprite aliasing not new; GameScreen browser test belongs in the unchanged list.
**Would change an approved decision**
- Structural `channel` type; two constructor overloads.

### Project standards
**Critical**
- `unicorn/consistent-destructuring` rejects destructuring `options` and then reading `options.x` (two errors on the plan's Timer constructor). Fix: read through `options` throughout.
- `perfectionist/sort-union-types` requires `{channel...}` before `{onComplete...}`.
- `prettier/prettier` rejects the 110-char ternary; canonical three-line `experimentalTernaries` form shown.
- Relocated `Tween<unknown>` comment exceeds `comment-length/limit-single-line-comments` (100) at class indent; re-wrap to four lines.
- Timer/Tween channel tests throw on a detached channel; use `channel.attach(new World())` + `channel.swap()` as in tests/EventChannel.test.ts.
**Important**
- Run both vitest projects; use `npm run typecheck`; list of dropped and stale comments; drop the `#elapsed` doc rewrite and the "in milliseconds" class-doc change; match existing test style.
**Minor**
- `() => {}` fine (`no-empty-function` off for TS); class member order otherwise correct; `sort-object-types`/`sort-objects` off; `in` narrowing verified; non-null assertions are warnings; `array-type: array-simple` makes both `Timer[]` and `Array<Tween<unknown>>` correct; padding rules satisfied.

### Feasibility, risks & test design
**Critical**
- Tween and Scheduler must migrate atomically or `Scheduler.tween` double-fires `onComplete`.
- Plan's constructor fails ESLint (sort-union-types, consistent-destructuring x2); lint-clean form verified.
- Timer channel test needs an attached channel; scaffolding spelled out.
- Browser project missing from verification; Playwright Chromium already installed.
- Snippets silently delete comments.
**Important**
- Union admits three silent mistakes (same fix); `#isFinished` position in Tween; Tween `#isFinished` test should assert return, target and single call; state the Sprite message verbatim; rename "emit" vocabulary in tests; Scheduler tween doc and comment relocation; skip cross-variant tests; `npm run typecheck`; lint includes Prettier.
**Minor**
- No import cycle; channel assignability via method bivariance; narrowing survives into the closure; wallHitPopup and popupCleanup tests untouched; tests/tsconfig.json irrelevant; `--project unit` confirmed (Scheduler.test.ts 9 passed).
- Proposed six-step order and a full tests/Scheduler.test.ts walkthrough (all 9 pass).

### Fresh perspective
**Critical**
- Union hole framed as a regression: today's nested `emit: {channel, event}` rejects a half pair; the flat union loses that. Same `never` fix, `options.channel` truthiness discriminant verified against the real `pixi.Container` Modal call sites.
**Important**
- Stale-comment sweep is much larger than the plan's one bullet.
- Tween-to-channel path has zero coverage; add a tweenSystem test.
- Add the symmetric "does not deliver twice" test for Timer.
- ECS loop gains re-entrancy a hook could abuse (splice from a hook corrupts the walk); one-line comment.
- Naming: `#pendingCompletion`, `#hasWarnedLoopCompletion`, `SpriteCompletion` (not adopted; plan keeps `#pending`, renames the flag to `#hasWarnedLoopEvent`).
**Minor**
- Repeat timer re-pushing one instance is safe (swap recycles arrays only); entity removed mid-timer unchanged; `clear()` mid-hook unchanged; only two `.show(` callers; detached-channel stack moves into an arrow; throw behaviour asymmetric between classes (pre-existing); Scheduler gains the ECS protocol; no layering problem; literal counts correct.

## Changes applied to plan
- Option union rewritten with `never` witnesses in both `TimerOptions` and `TweenOptions`; discriminant changed from `'channel' in options` to `options.channel === undefined`; decision recorded with the reason.
- Constructors rewritten without destructuring (`unicorn/consistent-destructuring`); union member order and Prettier ternary wrap fixed; Tween field order stated; comment-length re-wrap of the `Tween<unknown>` comment shown.
- Added decisions: completion-less instance legal; ECS convention for the event variant; state-before-delivery ordering; no comment removed, "emit" comments reworded. Added a "Reviewed and not adopted" list.
- Timer class doc now only fixes the stray space; `#elapsed` doc rewrite dropped; `update` docs tightened; ordering comment added.
- Scheduler section now shows every kept doc comment, the reworded `tween` doc, and a note on screen-side channel pushes.
- Sprite section now shows all three reset statements, both renames, the exact warning message and the comment rewording rule.
- Game section keeps the wallHitPopupSystem, tweenSystem and TweenComponent comments and adds the timerSystem next-frame and no-splice comment.
- New Sequencing section: six steps with the Tween+Scheduler, Sprite+playerActionSystem and components+systems atomic groups, EventEmit deleted last, and the full list of comments to reword.
- Tests section: attach-and-swap scaffolding, Timer at-most-once and type-level exclusivity tests, Tween contract test spelled out, new tweenSystem channel test, Sprite title rewording, GameScreen listed as unchanged.
- Verification now runs both vitest projects and `npm run typecheck`.
