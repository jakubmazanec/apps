---
'foam': patch
---
Dependency `@jakubmazanec/ts-utils` updated to version `^3.0.18`. Dependency `@react-router/fs-routes` updated to version `^8.3.0`. Dependency `@react-router/node` updated to version `^8.3.0`. Dependency `@react-router/serve` updated to version `^8.3.0`. Dependency `cross-env` updated to version `^10.1.0`. Dependency `isbot` updated to version `^5.2.1`. Dependency `lodash` updated to version `^4.18.1`. Dependency `react-router` updated to version `^8.3.0`. Dependency `zod` updated to version `^4.4.3`. Dependency `@react-router/dev` updated to version `^8.3.0`. Dependency `@types/lodash` updated to version `^4.17.24`. Dependency `@types/node` updated to version `^24.13.3`. Dependency `@vitest/coverage-v8` updated to version `^4.1.10`. Dependency `eslint` updated to version `^9.39.5`. Dependency `prettier` updated to version `^3.9.6`. Dependency `prettier-plugin-packagejson` updated to version `^2.5.22`. Dependency `typescript` updated to version `^6.0.3`. Dependency `vite` updated to version `^8.1.5`. Dependency `vitest` updated to version `^4.1.10`. Dependency `node` updated to version `^24.18.0`. Dependency `npm` updated to version `^11.18.0`.
